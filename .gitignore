# OS junk
.DS_Store
Thumbs.db

# Secrets — never commit these
.env
.env.local
*.key

# Logs and temp
*.log
tmp/

# Dependencies (we're not using npm, but just in case)
node_modules/

# Editor config
.vscode/
.idea/
*.swp
